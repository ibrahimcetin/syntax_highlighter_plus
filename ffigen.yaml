# Run with `dart run ffigen --config ffigen.yaml`.
name: OnigurumaBindings
description: |
  Bindings for `src/oniguruma-6.9.10/src/oniguruma.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/src/oniguruma_bindings_generated.dart'
headers:
  entry-points:
    - 'src/oniguruma-6.9.10/src/oniguruma.h'
  include-directives:
    - 'src/oniguruma-6.9.10/src/oniguruma.h'
comments:
  style: any
  length: full
